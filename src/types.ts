export type Company = {
  'change percent': string;
  high: string;
  low: string;
  name: string;
  price: string;
  symbol: string;
};

export type PossibleMatch = {
  name: string;
  symbol: string;
};
